const initialState = {
  guests: ['Tony Stark', 'Steve Rodgers', 'Nick Fury', 'Natasha Romanov', 'Clint Barton', 'Bruce Banner', 'Wanda Maximoff']
}

export default function reducer(state = initialState, action) {
  return state;
}
